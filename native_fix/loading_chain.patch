*** Begin Patch
*** Update File: cesium_godot/Utils/CurlHttpClient.h
@@
-#ifndef CURL_HTTP_CLIENT
-#define CURL_HTTP_CLIENT
-
-#include "godot_cpp/classes/os.hpp"
-#include <sstream>
-#if defined(CESIUM_GD_EXT)
-
-#ifdef _WIN32
-//Link to windows libraries
-#pragma comment(lib, "Ws2_32.Lib")
-#pragma comment(lib, "Wldap32.Lib")
-#pragma comment(lib, "Crypt32.Lib")
-#endif
-
-#include "godot_cpp/variant/string.hpp"
-#include "godot_cpp/core/error_macros.hpp"
-#include <godot_cpp/templates/vector.hpp>
-#include "godot_cpp/classes/http_client.hpp"
-using namespace godot;
-#elif defined(CESIUM_GD_MODULE)
-#include "core/templates/vector.h"
-#endif
-
-#include "BRThreadPool.h"
-#include <curl/curl.h>
-#include <vector>
-#include <string>
-#include <cctype>
-#include <cstring>
-
-using HighLevelResponseCallback_t = std::function<void(int32_t, const PackedByteArray&)>;
-using CesiumHeader_t = std::pair<std::string, std::string>;
-
-struct RequestHandle_t {
-	CURL *curlHandle;
-	bool available;
-
-	RequestHandle_t() : curlHandle(nullptr), available(true) {}
-
-	RequestHandle_t(const RequestHandle_t &other) {
-		this->curlHandle = other.curlHandle;
-		this->available = other.available;
-	}
-
-	RequestHandle_t &operator=(const RequestHandle_t &other) {
-		if (this != &other) {
-			this->curlHandle = other.curlHandle;
-			this->available = other.available;
-		}
-		return *this;
-	}
-
-	void easy_init() {
-		this->curlHandle = curl_easy_init();
-		this->available = true;
-		curl_easy_setopt(this->curlHandle, CURLOPT_SSL_VERIFYPEER, 0L);
-		curl_easy_setopt(this->curlHandle, CURLOPT_SSL_VERIFYHOST, 0L);
-		curl_easy_setopt(this->curlHandle, CURLOPT_FOLLOWLOCATION, 1);
-		curl_easy_setopt(this->curlHandle, CURLOPT_VERBOSE, 0);
-	}
-
-	void easy_cleanup() {
-		curl_easy_cleanup(this->curlHandle);
-	}
-
-	void configure_http_method(HTTPClient::Method method) {
-		switch (method) {
-			case HTTPClient::METHOD_POST:
-				curl_easy_setopt(this->curlHandle, CURLOPT_POST, 1);
-				break;
-			case HTTPClient::METHOD_PUT:
-				curl_easy_setopt(this->curlHandle, CURLOPT_UPLOAD, 1);
-				break;
-			default:
-				break;
-		}
-	}
-
-};
-
-struct MemoryStruct {
-	char *memory;
-	size_t size;
-};
-
-/// @brief Wrapper around libcurl, but without worrying about polling constantly
-template<uint32_t N_MAX_HANDLES>
-class CurlHttpClient {
-	static_assert(N_MAX_HANDLES <= 100); //Just to handle around 80 handles at once, multiple transfers will happen on the same handle
-
-public:
-	CurlHttpClient() {
-		if (s_activeInstances == 0) {
-			curl_global_init(CURL_GLOBAL_ALL);
-		}
-		s_activeInstances++;
-		this->m_activeHandles.reserve(N_MAX_HANDLES);
-		//Init all handles
-		for (int32_t i = 0; i < N_MAX_HANDLES; i++) {
-			RequestHandle_t handle{};
-			handle.easy_init();
-			this->m_activeHandles.push_back(handle);
-		}
-	}
-
-	~CurlHttpClient() {
-		for (int32_t i = 0; i < this->m_activeHandles.size(); i++) {
-			this->m_activeHandles[i].easy_cleanup();
-		}
-
-		s_activeInstances--;
-		if (s_activeInstances == 0) {
-			curl_global_cleanup();
-		}
-	}
-
-	void init_client(size_t maxThreads) {
-		this->m_threadPool.init(maxThreads);
-		std::string systemInfo = OS::get_singleton()->get_name().utf8().get_data();
-		auto architecture = OS::get_singleton()->get_processor_name().utf8().get_data();
-		std::stringstream stream;
-		stream << "Godot3DTiles/1.0 (" << systemInfo << "; " << architecture << ")";	
-		this->add_default_header({"User-Agent", stream.str()});
-	}
-
-	void send_get(const char* url, const HighLevelResponseCallback_t& callback, const std::vector<CesiumHeader_t>& headers) {
-		// Just send a request here
-		this->send_request(url, HTTPClient::METHOD_GET, callback, headers);
-	}
-
-	void send_get_same_thread(const char *url, const HighLevelResponseCallback_t &callback, const std::vector<CesiumHeader_t> &headers) {
-		// Just send a request here
-		this->send_request_same_thread(url, HTTPClient::METHOD_GET, callback, headers);
-	}
-
-	void send_request(const char* url, HTTPClient::Method method, const HighLevelResponseCallback_t& callback, const std::vector<CesiumHeader_t>& headers) {
-		//Get any active handle
-		int32_t handleIdx = this->get_available_handle_index();
-		this->m_activeHandles[handleIdx].available = false;
-		std::string urlCopy = url;
-		this->m_threadPool.enqueue([&, urlCopy, headers, callback, handleIdx] {
-			RequestHandle_t& handle = this->m_activeHandles[handleIdx];
-			handle.configure_http_method(method);
-			long responseCode;
-			PackedByteArray packedData = pull_url(urlCopy.c_str(), handle.curlHandle, &responseCode, headers);
-			//And call the callback methods here
-			callback(responseCode, packedData);
-			handle.available = true;
-		});
-	}
-
-
-	void send_request_same_thread(const char *url, HTTPClient::Method method, const HighLevelResponseCallback_t &callback, const std::vector<CesiumHeader_t> &headers) {
-		//Get any active handle
-		int32_t handleIdx = this->get_available_handle_index();
-		this->m_activeHandles[handleIdx].available = false;
-		RequestHandle_t &handle = this->m_activeHandles[handleIdx];
-		handle.configure_http_method(method);
-		long responseCode;
-		PackedByteArray packedData = pull_url(url, handle.curlHandle, &responseCode, headers);
-		//And call the callback methods here
-		callback(responseCode, packedData);
-		handle.available = true;
-	}
-
-	void add_default_header(const CesiumHeader_t& header) {
-		this->m_defaultHeaders.emplace_back(header);
-	}
-
-private:
-	static inline uint16_t s_activeInstances = 0;
-
-	PackedByteArray pull_url(const char *url, CURL *handle, long*outStatus , const std::vector<CesiumHeader_t> &headers) {
-		PackedByteArray buffer;
-		// Inline data: URIs (raster overlay tiles) must be decoded directly;
-		// libcurl cannot resolve the "data:" scheme (CURLE_URL_MALFORMAT).
-		if (is_data_uri(url)) {
-			*outStatus = 200;
-			return decode_data_uri(std::string(url));
-		}
-		//Options stuff
-		curl_easy_setopt(handle, CURLOPT_URL, url);
-		curl_easy_setopt(handle, CURLOPT_WRITEFUNCTION, &CurlHttpClient::write_callback);
-		curl_easy_setopt(handle, CURLOPT_WRITEDATA, &buffer);
-		// Do NOT send "Accept-Encoding: gzip". libcurl's gzip decode is broken in
-		// this build (curl error 61 / bad content-encoding => empty bodies from
-		// Cesium ion). Without advertising compression, servers respond identity
-		// encoded and no decode is needed. This makes ion/tileset/raster requests
-		// succeed at the cost of some extra bandwidth.
-		curl_easy_setopt(handle, CURLOPT_ACCEPT_ENCODING, nullptr);
-
-
-		curl_slist* curlHeaders = nullptr;
-		for (const CesiumHeader_t& h : headers)
-		{
-			std::string strHeader = h.first + ": ";
-			strHeader += h.second;
-			curlHeaders = curl_slist_append(curlHeaders, strHeader.c_str());
-		}
-
-		for (const CesiumHeader_t& h : headers) {
-			std::string strHeader = h.first + ": ";
-			strHeader += h.second;
-			curlHeaders = curl_slist_append(curlHeaders, strHeader.c_str());
-		}
-		
-		// Then add default headers
-
-		curl_easy_setopt(handle, CURLOPT_HTTPHEADER, curlHeaders);
-
-		//Perform the request		
-		CURLcode code = curl_easy_perform(handle);
-		//Then from the result we can do error handling
-		if (code != CURLcode::CURLE_OK) {
-			ERR_PRINT(String("Could not make request to: ") + url + String(" error: ") + itos(code));
-			return PackedByteArray();
-		}
-
-		curl_easy_getinfo(handle, CURLINFO_RESPONSE_CODE, outStatus);
-		curl_slist_free_all(curlHeaders);
-		return buffer;
-	}
-
-	static size_t write_callback(void *contents, size_t size, size_t nmemb, void *userp) {
-		auto *vectorBuffer = reinterpret_cast<PackedByteArray*>(userp);
-		char *contentCStr = reinterpret_cast<char *>(contents);
-		size_t realSize = size * nmemb;
-
-		for (size_t i = 0; i < realSize; i++) {
-			vectorBuffer->push_back(contentCStr[i]);
-		}
-	
-		return realSize;
-	}
-
-	// cesium-native can encode raster overlay tile images as inline "data:" URIs
-	// (e.g. data:image/png;base64,....). libcurl cannot resolve the "data:"
-	// scheme and fails with CURLE_URL_MALFORMAT, which produces an empty buffer
-	// and breaks image loading. Detect and decode these directly instead.
-	static bool is_data_uri(const char* url) {
-		if (!url) {
-			return false;
-		}
-		return std::strncmp(url, "data:", 5) == 0;
-	}
-
-	static int base64_value(unsigned char c) {
-		if (c >= 'A' && c <= 'Z') return c - 'A';
-		if (c >= 'a' && c <= 'z') return c - 'a' + 26;
-		if (c >= '0' && c <= '9') return c - '0' + 52;
-		if (c == '+') return 62;
-		if (c == '/') return 63;
-		return -1;
-	}
-
-	static PackedByteArray decode_base64(const std::string& input) {
-		PackedByteArray out;
-		int val = 0;
-		int valb = -8;
-		for (unsigned char c : input) {
-			if (c == '=') {
-				break;
-			}
-			if (std::isspace(c)) {
-				continue;
-			}
-			int d = base64_value(c);
-			if (d < 0) {
-				continue;
-			}
-			val = (val << 6) + d;
-			valb += 6;
-			if (valb >= 0) {
-				out.push_back(static_cast<uint8_t>((val >> valb) & 0xFF));
-				valb -= 8;
-			}
-		}
-		return out;
-	}
-
-	static PackedByteArray decode_data_uri(const std::string& url) {
-		// Format: data:[<mediatype>][;base64],<payload>
-		std::string rest = url.substr(5);
-		const bool isBase64 = rest.find(";base64") != std::string::npos;
-		const size_t comma = rest.find(',');
-		const std::string payload =
-		    (comma != std::string::npos) ? rest.substr(comma + 1) : rest;
-		if (isBase64) {
-			return decode_base64(payload);
-		}
-		// Non-base64 (percent-encoded) data URI - rare; decode %XX escapes.
-		PackedByteArray out;
-		for (size_t i = 0; i < payload.size(); ++i) {
-			if (payload[i] == '%' && i + 2 < payload.size()) {
-				const auto hexVal = [](char c) -> int {
-					if (c >= '0' && c <= '9') return c - '0';
-					if (c >= 'a' && c <= 'f') return c - 'a' + 10;
-					if (c >= 'A' && c <= 'F') return c - 'A' + 10;
-					return -1;
-				};
-				const int hi = hexVal(payload[i + 1]);
-				const int lo = hexVal(payload[i + 2]);
-				if (hi >= 0 && lo >= 0) {
-					out.push_back(static_cast<uint8_t>((hi << 4) | lo));
-					i += 2;
-					continue;
-				}
-			}
-			out.push_back(static_cast<uint8_t>(payload[i]));
-		}
-		return out;
-	}
-
-	int32_t get_available_handle_index() {
-		for (int32_t i = 0; i < this->m_activeHandles.size(); i++)
-		{
-			const RequestHandle_t &handle = this->m_activeHandles.at(i);
-			if (handle.available) {
-				return i;
-			}
-		}
-		RequestHandle_t handle{};
-		handle.easy_init();
-		this->m_activeHandles.push_back(handle);
-		return this->m_activeHandles.size() - 1;
-	}
-
-	//Have a thread pool for some batches of requests
-	BRThreadPool m_threadPool;
-	std::vector<CesiumHeader_t> m_defaultHeaders;
-	std::vector<RequestHandle_t> m_activeHandles;
-};
-
-#endif // HIGH_LEVEL_HTTP_CLIENT
+#ifndef CURL_HTTP_CLIENT
+#define CURL_HTTP_CLIENT
+#include "godot_cpp/classes/http_client.hpp"
+#include "godot_cpp/classes/project_settings.hpp"
+#include "godot_cpp/core/error_macros.hpp"
+#include "godot_cpp/variant/packed_byte_array.hpp"
+#include "BRThreadPool.h"
+#include <curl/curl.h>
+#include <chrono>
+#include <cstring>
+#include <cctype>
+#include <mutex>
+#include <string>
+#include <thread>
+#include <vector>
+using namespace godot;
+using HighLevelResponseCallback_t = std::function<void(int32_t, const PackedByteArray&)>;
+using CesiumHeader_t = std::pair<std::string, std::string>;
+
+// Each task exclusively owns its easy handle. No shared vector can invalidate
+// an in-flight handle, and no stack argument is captured by reference.
+template<uint32_t N_MAX_HANDLES>
+class CurlHttpClient {
+public:
+    CurlHttpClient() {
+        static std::once_flag initialized;
+        std::call_once(initialized, [] { curl_global_init(CURL_GLOBAL_ALL); });
+        m_caPath = ProjectSettings::get_singleton()->globalize_path(
+            "res://addons/cesium_godot/resources/cacert.pem").utf8().get_data();
+    }
+    void init_client(size_t maxThreads) { m_threadPool.init(maxThreads); }
+    void add_default_header(const CesiumHeader_t& h) { m_defaultHeaders.push_back(h); }
+    void send_get(const char* url, const HighLevelResponseCallback_t& cb, const std::vector<CesiumHeader_t>& headers) {
+        send_request(url, HTTPClient::METHOD_GET, cb, headers);
+    }
+    void send_get_same_thread(const char* url, const HighLevelResponseCallback_t& cb, const std::vector<CesiumHeader_t>& headers) {
+        send_request_same_thread(url, HTTPClient::METHOD_GET, cb, headers);
+    }
+    void send_request(const char* url, HTTPClient::Method method, const HighLevelResponseCallback_t& cb, const std::vector<CesiumHeader_t>& headers) {
+        m_threadPool.enqueue([this, urlCopy=std::string(url), method, cb, headers] {
+            send_request_same_thread(urlCopy.c_str(), method, cb, headers);
+        });
+    }
+    void send_request_same_thread(const char* url, HTTPClient::Method method, const HighLevelResponseCallback_t& cb, const std::vector<CesiumHeader_t>& headers) {
+        long status = 0;
+        PackedByteArray body = pull_url(url, method, status, headers);
+        cb(static_cast<int32_t>(status), body);
+    }
+private:
+    PackedByteArray pull_url(const char* url, HTTPClient::Method method, long& status, const std::vector<CesiumHeader_t>& headers) {
+        status = 0;
+        if (is_data_uri(url)) { status = 200; return decode_data_uri(url); }
+        CURLcode code = CURLE_OK;
+        PackedByteArray buffer;
+        const int maxAttempts = method == HTTPClient::METHOD_GET ? 4 : 1;
+        for (int attempt=0; attempt<maxAttempts; ++attempt) {
+            buffer.clear();
+            status = 0;
+            CURL* handle = curl_easy_init();
+            if (!handle) { code = CURLE_FAILED_INIT; break; }
+            curl_easy_setopt(handle, CURLOPT_URL, url);
+            curl_easy_setopt(handle, CURLOPT_FOLLOWLOCATION, 1L);
+            curl_easy_setopt(handle, CURLOPT_MAXREDIRS, 5L);
+            curl_easy_setopt(handle, CURLOPT_SSL_VERIFYPEER, 1L);
+            curl_easy_setopt(handle, CURLOPT_SSL_VERIFYHOST, 2L);
+            curl_easy_setopt(handle, CURLOPT_CAINFO, m_caPath.c_str());
+            curl_easy_setopt(handle, CURLOPT_CONNECTTIMEOUT, 10L);
+            curl_easy_setopt(handle, CURLOPT_TIMEOUT, 30L);
+            curl_easy_setopt(handle, CURLOPT_NOSIGNAL, 1L);
+            curl_easy_setopt(handle, CURLOPT_HTTP_VERSION, CURL_HTTP_VERSION_1_1);
+            curl_easy_setopt(handle, CURLOPT_WRITEFUNCTION, &CurlHttpClient::write_callback);
+            curl_easy_setopt(handle, CURLOPT_WRITEDATA, &buffer);
+            // This build uses Cesium's GunzipAssetAccessor for compressed data.
+            curl_easy_setopt(handle, CURLOPT_ACCEPT_ENCODING, nullptr);
+            if (method == HTTPClient::METHOD_POST) curl_easy_setopt(handle, CURLOPT_POST, 1L);
+            else if (method == HTTPClient::METHOD_PUT) curl_easy_setopt(handle, CURLOPT_UPLOAD, 1L);
+            curl_slist* list = nullptr;
+            for (const auto& h : m_defaultHeaders) list = curl_slist_append(list, (h.first+": "+h.second).c_str());
+            for (const auto& h : headers) list = curl_slist_append(list, (h.first+": "+h.second).c_str());
+            curl_easy_setopt(handle, CURLOPT_HTTPHEADER, list);
+            code = curl_easy_perform(handle);
+            if (code == CURLE_OK) curl_easy_getinfo(handle, CURLINFO_RESPONSE_CODE, &status);
+            curl_easy_setopt(handle, CURLOPT_HTTPHEADER, nullptr);
+            curl_slist_free_all(list);
+            curl_easy_cleanup(handle);
+            const bool transient = code == CURLE_COULDNT_RESOLVE_HOST || code == CURLE_COULDNT_CONNECT ||
+                code == CURLE_OPERATION_TIMEDOUT || code == CURLE_SSL_CONNECT_ERROR ||
+                code == CURLE_GOT_NOTHING || code == CURLE_SEND_ERROR || code == CURLE_RECV_ERROR ||
+                code == CURLE_PARTIAL_FILE || status == 408 || status == 429 || (status >= 500 && status <= 599);
+            if (!transient || attempt+1 == maxAttempts) break;
+            std::this_thread::sleep_for(std::chrono::milliseconds(250 * (1 << attempt)));
+        }
+        if (code != CURLE_OK) {
+            status = 0; // Never return an uninitialized HTTP code or partial body.
+            buffer.clear();
+            ERR_PRINT(String("Cesium download failed after retries: curl=")+itos(code));
+        }
+        return buffer;
+    }
+    static size_t write_callback(void* contents, size_t size, size_t nmemb, void* userp) {
+        auto* buffer = static_cast<PackedByteArray*>(userp);
+        const size_t bytes = size * nmemb;
+        const int64_t offset = buffer->size();
+        if (buffer->resize(offset + bytes) != OK) return 0;
+        std::memcpy(buffer->ptrw() + offset, contents, bytes);
+        return bytes;
+    }
+	static bool is_data_uri(const char* url) {
+		if (!url) {
+			return false;
+		}
+		return std::strncmp(url, "data:", 5) == 0;
+	}
+
+	static int base64_value(unsigned char c) {
+		if (c >= 'A' && c <= 'Z') return c - 'A';
+		if (c >= 'a' && c <= 'z') return c - 'a' + 26;
+		if (c >= '0' && c <= '9') return c - '0' + 52;
+		if (c == '+') return 62;
+		if (c == '/') return 63;
+		return -1;
+	}
+
+	static PackedByteArray decode_base64(const std::string& input) {
+		PackedByteArray out;
+		int val = 0;
+		int valb = -8;
+		for (unsigned char c : input) {
+			if (c == '=') {
+				break;
+			}
+			if (std::isspace(c)) {
+				continue;
+			}
+			int d = base64_value(c);
+			if (d < 0) {
+				continue;
+			}
+			val = (val << 6) + d;
+			valb += 6;
+			if (valb >= 0) {
+				out.push_back(static_cast<uint8_t>((val >> valb) & 0xFF));
+				valb -= 8;
+			}
+		}
+		return out;
+	}
+
+	static PackedByteArray decode_data_uri(const std::string& url) {
+		// Format: data:[<mediatype>][;base64],<payload>
+		std::string rest = url.substr(5);
+		const bool isBase64 = rest.find(";base64") != std::string::npos;
+		const size_t comma = rest.find(',');
+		const std::string payload =
+		    (comma != std::string::npos) ? rest.substr(comma + 1) : rest;
+		if (isBase64) {
+			return decode_base64(payload);
+		}
+		// Non-base64 (percent-encoded) data URI - rare; decode %XX escapes.
+		PackedByteArray out;
+		for (size_t i = 0; i < payload.size(); ++i) {
+			if (payload[i] == '%' && i + 2 < payload.size()) {
+				const auto hexVal = [](char c) -> int {
+					if (c >= '0' && c <= '9') return c - '0';
+					if (c >= 'a' && c <= 'f') return c - 'a' + 10;
+					if (c >= 'A' && c <= 'F') return c - 'A' + 10;
+					return -1;
+				};
+				const int hi = hexVal(payload[i + 1]);
+				const int lo = hexVal(payload[i + 2]);
+				if (hi >= 0 && lo >= 0) {
+					out.push_back(static_cast<uint8_t>((hi << 4) | lo));
+					i += 2;
+					continue;
+				}
+			}
+			out.push_back(static_cast<uint8_t>(payload[i]));
+		}
+		return out;
+	}
+
+
+    std::string m_caPath;
+    std::vector<CesiumHeader_t> m_defaultHeaders;
+    // Destroy/join the workers before destroying data used by pending tasks.
+    BRThreadPool m_threadPool;
+};
+#endif
*** Update File: cesium_godot/Models/CesiumGDTileset.cpp
@@
 	if (!foundNode->is_inside_tree()) {
+		foundNode->set_meta("preview_geometric_error", tile.getGeometricError());
 		size_t hash = std::visit(CesiumVariantHash{}, tile.getTileID());
*** End Patch

*** Begin Patch
*** Update File: cesium_godot/Models/CesiumGDTileset.cpp
@@
 #include <type_traits>
+#include <unordered_set>
@@
-	for (CesiumUtility::IntrusivePointer<const Cesium3DTilesSelection::Tile> tile : updateResult.tilesToRenderThisFrame) {
+	std::unordered_set<const void*> selectedResources;
+	for (CesiumUtility::IntrusivePointer<const Cesium3DTilesSelection::Tile> tile : updateResult.tilesToRenderThisFrame) {
+		const auto* content = tile->getContent().getRenderContent();
+		if (content) selectedResources.insert(content->getRenderResources());
 		this->render_tile_as_node(*tile);
 	}
+	// Synchronize against the complete selection, not just a transition list.
+	// Cached ancestors must not remain visible over their refined children.
+	for (int i = 0; i < get_child_count(); ++i) {
+		auto* node = Object::cast_to<Cesium3DTile>(get_child(i));
+		if (!node || !node->is_visible() || selectedResources.count(node)) continue;
+		node->hide();
+		if (m_createPhysicsMeshes && node->get_child_count() > 0) {
+			Node* collision = node->get_child(0);
+			if (collision->get_child_count() > 0) {
+				auto* shape = Object::cast_to<CollisionShape3D>(collision->get_child(0));
+				if (shape) shape->set_disabled(true);
+			}
+		}
+	}
*** End Patch
